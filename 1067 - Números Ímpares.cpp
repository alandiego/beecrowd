#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base :: sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    int X;
    cin >> X;

    for (int i = 1; i <= X; i += 2) {
        cout << i << "\n";
    }

    return 0;
}

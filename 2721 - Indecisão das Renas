#include <bits/stdc++.h>

using namespace std;

int main(){
    vector<string> renas = {"Dasher", "Dancer", "Prancer", "Vixen", "Comet", 
    "Cupid", "Donner", "Blitzen", "Rudolph"};
    
    int total=0, A;
    
    for (int i=0; i<9; i++){
        cin >> A;
        total = (total+A)%9;
    }
    total = (total+8)%9;
    cout << renas[total] << endl;
    
    return 0;
}
